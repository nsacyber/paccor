rootProject.name = 'paccor'

include('doc-tools')
project(':doc-tools').projectDir = file('docs/doc-tools')

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}
